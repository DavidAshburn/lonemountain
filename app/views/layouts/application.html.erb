<!DOCTYPE html>
<html>
  <head>
    <title>Lone Mountain Society</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Aboreto&family=Cormorant+Garamond:wght@400;700&family=Josefin+Slab&display=swap" rel="stylesheet">
  </head>

   <% if notice %>
    <div class="flex flex-row absolute left-10 bottom-10">
      <%= render 'layouts/notice' %>
    </div>
  <% end %>
  
  <% if alert %>
    <div class="flex flex-row absolute left-10 bottom-10">
      <%= render 'layouts/alert' %>
    </div>
  <% end %>

  <body class="bg-fixed bg-oversize bg-gradient-to-bl from-yellow-100 via-amber-200 to-orange-300">
    <main class="flex flex-col w-[100vw]">
      <!--
      <div class="ml-5 mt-5 w-11/12">
        <%= render 'layouts/nav' %>
      </div>
      -->
      <%= yield %>
    </main>
  </body>
</html>